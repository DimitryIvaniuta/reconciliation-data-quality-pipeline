# reconciliation-data-quality-pipeline
Production-grade Java 25 and Spring Boot reactive reconciliation pipeline that compares Kafka events with PostgreSQL records and aggregates, detects data-quality mismatches, stores auditable reports, sends alerts, and supports resumable replay and backfill.
